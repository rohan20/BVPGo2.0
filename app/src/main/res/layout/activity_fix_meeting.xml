<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".NavigationDrawer.FixMeetingActivity">

    <TextView
        android:id="@+id/selectTeacherText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:fontFamily="serif"
        android:text="Select teacher"
        android:textSize="20sp"
        android:textStyle="bold|italic" />

    <Spinner
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/selectTeacherText"
        android:id="@+id/teachersSpinner"
        android:layout_centerHorizontal="true"
        android:layout_margin="15dp" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/teachersSpinner"
        android:layout_margin="15dp"
        android:text="Subject of meeting: "
        android:fontFamily="serif"
        android:textSize="20sp"
        android:id="@+id/subjectText"
        android:textStyle="bold|italic"
        />

    <EditText
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:fontFamily="serif"
        android:textSize="15sp"
        android:background="@android:color/background_light"
        android:inputType="textCapSentences|textMultiLine"
        android:layout_centerHorizontal="true"
        android:layout_margin="15dp"
        android:scrollHorizontally="false"
        android:layout_below="@id/subjectText"
        android:padding="5dp"
        android:gravity="top"
        android:id="@+id/subjectEditText"
        />

    <Button
        android:layout_width="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_height="wrap_content"
        android:text="Send"
        android:id="@+id/sendButton"
        android:layout_below="@id/subjectEditText"
        />
</RelativeLayout>
